import{Minus,Plus,Trash}from'lucide-react';import{useCart}from'../context/CartContext';export default function CartItem({item}){const{update,remove}=useCart();return <div className="glass flex items-center justify-between rounded-2xl p-4"><div><h4 className="font-black text-monsoon-blue">{item.name}</h4><p className="text-sm text-gray-600">₹{item.price} · {item.restaurant_name}</p></div><div className="flex items-center gap-3"><button onClick={()=>update(item.id,item.quantity-1)}><Minus/></button><b>{item.quantity}</b><button onClick={()=>update(item.id,item.quantity+1)}><Plus/></button><button onClick={()=>remove(item.id)} className="text-red-500"><Trash/></button></div></div>}
