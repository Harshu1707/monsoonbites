import{Search}from'lucide-react';export default function SearchBar({value,onChange,placeholder='Search restaurants or dishes'}){return <div className="glass flex items-center gap-3 rounded-full p-2 shadow-glass"><Search className="ml-3 text-monsoon-blue"/><input className="flex-1 bg-transparent p-3 outline-none" value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder}/></div>}
