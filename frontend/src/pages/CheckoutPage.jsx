import{useState}from'react';import{useNavigate}from'react-router-dom';import{useCart}from'../context/CartContext';import{api}from'../services/api';export default function CheckoutPage(){const[delivery_address,setAddress]=useState('');const[paid,setPaid]=useState(false);const{cart,load}=useCart();const nav=useNavigate();const submit=async(e)=>{e.preventDefault();await api.post('/orders/checkout',{delivery_address});await load();nav('/orders')};return <main className="mx-auto max-w-4xl px-4 py-10"><h1 className="text-4xl font-black text-monsoon-blue">Checkout</h1><form onSubmit={submit} className="glass mt-6 rounded-3xl p-6"><textarea className="input min-h-28" placeholder="Delivery address" value={delivery_address} onChange={e=>setAddress(e.target.value)}/><div className="my-5 rounded-2xl bg-white p-5"><b>Mock payment</b><p>Order total: ₹{cart.total.toFixed(0)}</p><button type="button" onClick={()=>setPaid(true)} className="btn mt-3 !bg-monsoon-green">{paid?'Payment authorized':'Pay securely'}</button></div><button disabled={!paid} className="btn disabled:opacity-40">Place order</button></form></main>}
