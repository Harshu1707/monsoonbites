import{useState}from'react';import{useAuth}from'../context/AuthContext';import{api}from'../services/api';export default function ProfilePage(){const{user,setUser}=useAuth();const[form,setForm]=useState(user||{});const save=async(e)=>{e.preventDefault();const{data}=await api.put('/auth/profile',form);setUser(data.user);localStorage.setItem('user',JSON.stringify(data.user))};return <main className="mx-auto max-w-xl px-4 py-10"><h1 className="text-4xl font-black text-monsoon-blue">Profile</h1><form onSubmit={save} className="glass mt-6 rounded-3xl p-6"><input className="input" value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input mt-4" value={form.email||''} onChange={e=>setForm({...form,email:e.target.value})}/><button className="btn mt-5">Save profile</button></form></main>}
